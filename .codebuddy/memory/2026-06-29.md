# 2026-06-29

## SnPlayer Flutter Android 构建修复

两个问题：
1. **AGP 9.0.1 不兼容** — 降级至 8.7.3，Kotlin 降至 2.0.21
2. **video_thumbnail 插件 jcenter() 废弃** — 修补 pub 缓存中的 build.gradle，删除 buildscript 块 + jcenter()→mavenCentral()
3. **Gradle 8.10.2 下载失败**（网络问题）— 改用本地已缓存的 gradle-8.14.3-bin

修改文件：android/settings.gradle.kts、gradle-wrapper.properties、gradle.properties、video_thumbnail 缓存插件
