# 2026-06-29

## SnPlayer Flutter Android 构建修复

两个问题：
1. **AGP 9.0.1 不兼容** — 降级至 8.7.3，Kotlin 降至 2.0.21
2. **video_thumbnail 插件 jcenter() 废弃** — 修补 pub 缓存中的 build.gradle，删除 buildscript 块 + jcenter()→mavenCentral()
3. **Gradle 8.10.2 下载失败**（网络问题）— 改用本地已缓存的 gradle-8.14.3-bin

修改文件：android/settings.gradle.kts、gradle-wrapper.properties、gradle.properties、video_thumbnail 缓存插件

## Flutter 编译错误修复（CardTheme + import 缺失 + 版本升级）

4 个问题：
1. **CardTheme → CardThemeData** — lib/main.dart:36，新版 Flutter 的 ThemeData.cardTheme 参数类型变更为 CardThemeData
2. **path_provider_service.dart 缺少 import** — 使用了 lockVideoDirName/unlockVideoDirName/playCacheDirName 但未导入 ../config/crypto.dart
3. **safe_delete_helper.dart 缺少 import** — 使用了 Uint8List 但未导入 dart:typed_data
4. **AGP/Kotlin 版本过低警告** — AGP 8.7.3→8.11.1，Kotlin 2.0.21→2.2.20（Gradle 9.1.0 满足 AGP 8.11.x 最低要求 8.13+）

修改文件：lib/main.dart、lib/services/path_provider_service.dart、lib/services/safe_delete_helper.dart、android/settings.gradle.kts
