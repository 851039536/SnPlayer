org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Workaround for Kotlin 2.2.20 incremental compilation bug on Windows
# "Storage is already registered" when sources are on different drives (C: pub cache vs E: project)
# Forces Kotlin compiler to run in-process instead of using a daemon
kotlin.compiler.execution.strategy=in-process
